{
  "name": "restaurant-app",
  "version": "1.0.0",
  "description": "Restaurant Management Application",
  "scripts": {
    "start": "concurrently \"npm run start:backend\" \"npm run start:frontend\"",
    "start:backend": "python db_cloud_connection/api.py",
    "start:frontend": "cd frontend-react && npm start",
    "install:all": "npm install && cd frontend-react && npm install"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
